  <div class="field">
    <%= f.hidden_field :character_id %>
    <%= f.label :weapon, "Weapon:" %>
    <%= f.collection_select(:weapon_id, Weapon.all, :id, :weapon_name, :prompt => true) %>
    <%= f.label :weapon, "Mastercraft:" %>
    <%= f.check_box :mastercraft %>
    <%= f.link_to_remove "remove" %>
  </div>
